[workspace]
members = [
    "cdec", 
    "cli",
    "cmd", 
    "ecco",
    "my_log",
    "utils", 
    "yew-avin_a_laf", 
    "yew-nani", 
    "yew-wot_m8", 
    "yew-wu"]

[workspace.package]
version = "1.0.10"

[profile.release]
debug = true
opt-level = 3               # Use slightly better optimizations.
overflow-checks = false     # Disable integer overflow checks.
# lto = true
incremental = true
panic = "unwind"
strip = "none"
